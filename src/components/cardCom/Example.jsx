const Example = (propss) => {
  // console.log(propss);
  // console.log(propss.children);

  // children props data receiving
  return (
    <div>
      <h2>hello example</h2>
      {propss.children}
    </div>
  );
};

export default Example;
